from django.apps import AppConfig


class VforceConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'  # Configura el tipo de campo automático predeterminado
    name = 'vforce'  # Nombre de la aplicación